<?xml version="1.0" encoding="utf-8"?>
<!--
  桌面图标的底色。与 PixelPalette.Ground（#0B0C10）同一个值 —— 图标的底和 App
  的底是同一块颜色，点进去不会有一次明暗跳变。

  为什么不用透明：adaptive icon 的背景层不允许透明（系统要拿它做视差），
  透明会被渲染成白色，深色图标在白底上就成了另一个东西。
-->
<resources>
    <color name="ic_launcher_background">#FF0B0C10</color>
</resources>
